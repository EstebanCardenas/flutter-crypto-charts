part of charts_ui.widgets;

class AssetHistoryChart extends StatelessWidget {
  const AssetHistoryChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
